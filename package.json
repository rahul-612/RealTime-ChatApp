{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "RealTime Chat App built using NodeJs SocketIO",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"node app.js",
    "build":"cd client && npm run build"
  },
  "author": "rahul",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "nodemon": "^2.0.12",
    "socket.io": "^4.1.3"
  }
}
